function [Hd,b] = filtdesign (N  ,Fpass ,Fstop);  % Stopband Weight
%FILTDESIGN_2 Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 23.2 and Signal Processing Toolbox 23.2.
% Generated on: 07-Jul-2024 22:35:37

% FIR least-squares Lowpass filter designed using the FIRLS function.

% All frequency values are normalized to 1.

% N     = 10;    % Order
% Fpass = 0.05;  % Passband Frequency
% Fstop = 0.33;  % Stopband Frequency
Wpass = 1;     % Passband Weight
 Wstop = 40;    % Stopband Weight

% Calculate the coefficients using the FIRLS function.
b  = firls(N, [0 Fpass Fstop 1], [1 1 0 0], [Wpass Wstop]);
Hd = dfilt.dffir(b);

b=fi(b,1,16,14);
% [EOF]
